{{- range .Values.storageClass }}
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .name }}
  annotations:
    storageclass.kubernetes.io/is-default-class: {{ .default | default true | quote }}
provisioner: {{ .provisioner }}
reclaimPolicy: {{ .reclaimPolicy }} # default value is Delete
allowVolumeExpansion: {{ .allowVolumeExpansion }}
mountOptions:
    {{- range .mountOptions }}
    - {{ . }}
    {{- end }} 
volumeBindingMode: {{ .volumeBindingMode }}
parameters:
  guaranteedReadWriteLatency: {{ .guaranteedReadWriteLatency | quote }} # provider-specific
{{- end }}